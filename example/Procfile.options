---
# Nullable
# auto generate
appName:

# Nullable
# auto get from work directory where cmd is executed
workDir:

# Nullable
# auto get from work directory where cmd is executed
procfile:

# Nullable
#env:
#    - PATH=/usr/local/bin:$PATH

# Nullable
# auto generate from Procfile
processes:
    web:
        root:
        pidRoot:
        logRoot:
        # Supported signal: TERM QUIT INT
        #stopSignal: INT
        #env:
        #    - PORT=3000
